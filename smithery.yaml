name: mcp-carsearch
language: python
build:
  requirements: requirements.txt

run:
  entrypoint: app.main:app  # FastAPI 인스턴스 경로 (파일:변수)
  command: uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}
  env:
    PORT: 8000

python:
  version: "3.10"