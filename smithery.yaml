startCommand:
  type: sse
  configSchema: {}
  commandFunction: |
    function(config) {
      return {
        command: "uvicorn",
        args: ["app.main:app", "--host", "0.0.0.0", "--port", "8000"],
        env: {}
      };
    }